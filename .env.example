# Optional: Add a secret for cron job authentication
# Generate with: openssl rand -base64 32
CRON_SECRET=your-secret-here

# Optional: Add if you want to use a different data source
# Currently uses Yahoo Finance which doesn't require an API key
